<Window x:Class="UgCS.SDK.Examples.UploadRouteFromSelectedWaypoint.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Upload route from the selected waypoint example" Height="450" Width="800"
        Loaded="Window_Loaded" Closing="Window_Closing"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStartupLocation="CenterScreen"
        MinWidth="800"
        MinHeight="600"
        >

    <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Source="ConnectingScreen.xaml" LoadCompleted="MainFrame_LoadCompleted"/>
</Window>
